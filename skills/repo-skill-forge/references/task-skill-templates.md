# Task Skill Templates

Authoritative spec for each skill generated by `repo-skill-forge`.
For each skill type: content sources, section structure, quality checks.

**`{repo-slug}` in every template below** = the repo name lowercased, with every
run of non-`[a-z0-9-]` chars replaced by a single hyphen, trimmed of leading/
trailing hyphens (e.g. `acme_app` → `acme-app`). Every generated skill's
folder AND its frontmatter `name` MUST equal `{repo-slug}-<skill>` — agentskills
names are lowercase letters/numbers/hyphens only and MUST equal the folder. A
colon-namespaced name (`wicked-understanding:fix-bug`) is invalid and won't install.

The generated skills progressively load deeper context from
`../{repo-slug}-wiki/refs/` — the wiki backing store built by `repo-wiki-planner`,
which installs as a sibling folder named `{repo-slug}-wiki`.

---

## Read first: these examples are ILLUSTRATIVE, not prescriptive

The code blocks below skew toward one stack (TypeScript / REST / handler →
service → repository with a DI container and an ORM). That is **one** shape, not
the default. Before writing any skill, read `survey.md` + the four lenses and
pin down the repo's actual paradigm:

- **Language** — TS, Go, Rust, Python, Java, …
- **Style** — OO vs functional-core vs procedural
- **Shape** — web service vs CLI vs library vs batch/pipeline
- **Persistence** — has-DB (+ORM/migrations) vs no-DB / in-memory / file-backed
- **Layout** — layered (transport/app/domain/infra) vs flat / module-per-feature

Map every section to **that** reality. If the repo has no controllers, no DI
container, no ORM, and no migrations, **do not invent them** — describe the real
structure instead:

| Stack | "Add a unit of work" reality |
|---|---|
| Go (CLI/server) | new `<name>_tool.go` (or `<name>.go`) + register in `server.go` / `main.go`; build with `go build ./...` |
| Rust | new module + `impl Trait for T`, wire in `mod.rs` / `lib.rs`; build/test with `cargo` |
| Python (functional core) | new pure module, wired explicitly at the composition root; no DI annotations |
| TS/NestJS (the example below) | controller → service → repo + provider registration |

A skill that cites migrations, a DI container, or controllers in a repo that has
none is **filler, not a default** — it fails the "no name-swappable sentences"
bar and makes the skill actively wrong. When the survey contradicts a template
section, follow the survey and drop or rewrite the section.

---

## Table of Contents

1. [fix-bug](#fix-bug)
2. [add-feature](#add-feature)
3. [add-domain](#add-domain)
4. [write-tests](#write-tests)
5. [scaffold](#scaffold)
6. [Shared: "Load for deeper context" table](#shared-load-for-deeper-context)
7. [Shared: description formula](#shared-description-formula)

---

## fix-bug

**When it fires**: agent is investigating an error, unexpected behavior,
failing test, or production incident.

**Always-in-context goal**: agent can immediately identify which layer and
file to look in based on the symptom — without reading any code.

### Content sources

| Section | Source artifact | Source section |
|---|---|---|
| Triage table | `architecture.md` | Layers table + Component Map |
| Error handling chain | `technical.md` | Error Handling section |
| Test commands | `ops.md` | Daily Development Commands table |
| Common bug sources | `technical.md` | Non-Obvious Technical Details + Gotchas |
| Diagnostic checks | `ops.md` | Debugging & Observability section |

### Generated SKILL.md structure

```markdown
---
name: {repo-slug}-fix-bug
description: >
  Bug-fixing workflow for {repo-name} — a {type} built with {stack}.
  Use when investigating any bug, error, unexpected behavior, or failing test.
  Covers: symptom-to-layer triage for the {arch-style} architecture, the
  {error-class} error chain from throw to HTTP response, test verification
  with {test-framework}, and {N} common bug patterns in this codebase.
  Load this before touching code when debugging {repo-name}.
---

# {repo-name} — Fix a Bug

## Symptom → layer triage

Start here. Map what you're seeing to where it lives. **Columns and rows come
from architecture.md's actual layers/modules — the rows below are a layered-web
example; for a flat or CLI repo, use its real units (commands, packages,
modules) and drop the "Layer" column if there are no layers.**

Layered web-service example:

| Symptom | First file(s) to open | Layer | What to look for |
|---|---|---|---|
| HTTP 4xx response | `{handlers-path}` | Transport | error construction, guard clauses |
| Validation failure | `{schemas-path}` | Application | schema constraints, required fields |
| Wrong result / bad data | `{domain-path}` | Domain | business rules, invariant checks |
| Unexpected null / missing field | `{repos-path}` | Infrastructure | query filters, JOIN conditions |

Go CLI/server example (no layers — map by command/package):

| Symptom | First file(s) to open | What to look for |
|---|---|---|
| Wrong CLI output / exit code | `cmd/{name}/main.go` | flag parsing, error returns |
| Bad business result | `internal/{pkg}/*.go` | the function that owns this rule |
| Tool not found / not registered | `server.go` | the registration list |

(Populate the real rows from architecture.md layers/modules + technical.md gotchas)

## Error flow (origin → surface)

Trace an error from where it originates to what the caller sees. **Adapt the
mechanism to this repo: exceptions → handler/filter for OO web apps; `Result` /
`error` return values threaded up the call stack for Rust/Go; an `Either` /
tagged-union for functional cores.** If there is no central handler, say so and
show the actual propagation.

Exception + HTTP example:

```
{ErrorClass} thrown in {domain-path}
  └─▶ caught by {middleware/filter} in {path}
        └─▶ mapped to HTTP {status} + {"error": {"code": "...", "message": "..."}}
```

Go/Rust `error`/`Result` example:

```
fn / func in {core-path} returns Err/error
  └─▶ propagated with `?` / `if err != nil { return err }` up to {entrypoint}
        └─▶ printed to stderr + non-zero exit / mapped to {surface}
```

Error type hierarchy (if one exists): `{BaseError}` → `{NotFoundError}`,
`{ValidationError}`, … Defined in: `{errors-path}`. For Result/error stacks,
name the error enum or sentinel values instead.

## Diagnostic commands

```bash
{test-all}                        # run all tests
{test-specific} {file-or-pattern} # run tests for one file
{log-debug-command}               # enable debug logging
{health-endpoint}                 # verify service is up
```

## Common bug patterns in this repo

From past analysis — check these before digging further:

1. **{Pattern name}**: {what causes it and where it shows up}
   → check `{file}`

2. **{Pattern name}**: {what causes it and where}
   → check `{file}`

3. **{Pattern name}**: {what causes it and where}
   → check `{file}`

(From technical.md "Non-Obvious Technical Details" and ops.md "Common Issues")

## Load for deeper context

(see Shared section below)
```

### Quality checks

- [ ] Triage table has ≥ 4 rows covering the repo's real units (layers if it
      has them; otherwise commands / packages / modules)
- [ ] Every "First file(s)" cell has a real path from the analysis
- [ ] Error flow matches this repo's real mechanism (exceptions vs `Result`/`error`
      vs `Either`) — no invented middleware/filter if there is none
- [ ] All commands are copy-pasteable (from ops.md — no placeholders)
- [ ] Bug patterns are real (from technical.md gotchas, not invented)

---

## add-feature

**When it fires**: agent is implementing new functionality — a new endpoint,
a new user action, a new business operation.

**Always-in-context goal**: agent can follow the implementation sequence
for this repo end-to-end without needing to look at other files for guidance.

### Content sources

| Section | Source artifact | Source section |
|---|---|---|
| Implementation sequence | `technical.md` | Adding New Features → New API endpoint |
| Conventions table | `technical.md` | Conventions |
| Wiring guide | `architecture.md` | Cross-Cutting Concerns + DI/module registration |
| Test pattern | `technical.md` | Testing Patterns |
| Related domain context | `domain.md` | Core Operations (one example operation) |

### Generated SKILL.md structure

```markdown
---
name: {repo-slug}-add-feature
description: >
  Feature-addition workflow for {repo-name} — a {type} built with {stack}.
  Use when implementing new functionality: new endpoints, user actions, or business
  operations. Covers: the {N}-step implementation sequence for this {arch-style}
  codebase, naming conventions, DI wiring, and test patterns.
  Load when you know WHAT to build and need to know HOW to build it here.
---

# {repo-name} — Add a Feature

## Implementation sequence

Follow in order. Each step depends on the previous. **The steps below are a
layered TS/REST example. Derive the real sequence from technical.md "Adding New
Features" by tracing one existing feature end-to-end in THIS repo — the number
of steps, the files touched, and the wiring step all change with the paradigm.**
Two contrasting shapes:

- **Go CLI tool**: write `<name>_tool.go` (handler + logic, often one file) →
  register it in `server.go` / `main.go` → add a test → `go build ./...`. No
  schema layer, no DI container, no separate repository.
- **Rust library**: add a module → `impl` the relevant trait → re-export in
  `lib.rs` / `mod.rs` → `cargo test`. Wiring is `mod` declarations, not a DI
  registration call.

### Step 1: Define the contract
- Entry point: `{routes-path}` — add `router.{method}('{path}', handler)` (REST),
  a subcommand registration (CLI), or an exported function signature (library)
- Input shape: `{schemas-path}` — `{FeatureName}RequestSchema` (Zod), a struct
  with `#[derive(Deserialize)]` (Rust), a typed param (Go), or a dataclass (Python)
- Output shape: `{schemas-path}` — the response/return type
- Reference: `{example-route-file}` for the existing pattern

### Step 2: Write the handler / entry function
- File: `{handlers-path}/{feature-name}.{ext}` (web), `{name}_tool.go` (Go CLI),
  or the relevant module (library)
- Extends/implements: `{BaseHandler}` or the repo's actual base/trait — **omit
  this line if the repo uses plain functions**
- Responsibilities: parse + validate input → call core logic → map output
- Reference: `{example-handler-file}`

### Step 3: Write the core logic / use-case
- File: `{services-path}/{feature-name}.{ext}` (or the package/module that owns this)
- Responsibilities: auth checks, orchestrate domain, side effects
- Reference: `{example-service-file}`

### Step 4: Domain logic (if new business rule)
- File: `{domain-path}/{area}/{feature-name}.{ext}`
- Rule: pure functions only, no I/O imports (if the repo separates a domain core)
- Reference: `{example-domain-file}`

### Step 5: Wire it in
- **Only if this repo has explicit wiring.** DI container: register at `{di-path}`.
  Go: add to the registration slice in `server.go`. Rust/Python: add the `mod` /
  import at the composition root. **If features are discovered automatically or
  there is no wiring step, delete this step — do not invent a DI container.**
- Follow the pattern in: `{example-registration}`

### Step 6: Test
- File: `{test-path}/{feature-name}.{test-ext}` (co-located, in `{test-dir}`, or
  a `#[cfg(test)]` module for Rust)
- Use `{FactoryName}` for test data (if the repo has fixtures/factories)
- Run: `{test-specific-command} {feature-name}`

## Conventions

| Aspect | Convention | Example |
|---|---|---|
| Files | {file-naming} | `{example}` |
| Functions | {function-naming} | `{example}` |
| Types | {type-naming} | `{example}` |
| Errors | {error-pattern} | `{example — the repo's real idiom: `throw new {ErrorClass}(…)`, `return fmt.Errorf(…)`, `Err({Error}::…)`, or `raise {Error}(…)`}` |

## Load for deeper context

(see Shared section below)
```

### Quality checks

- [ ] Implementation sequence is in the correct order for this specific repo
- [ ] Every "File:" entry has a real path
- [ ] Every "Reference:" entry names a real existing file
- [ ] Conventions table comes from technical.md verbatim
- [ ] Test step names the real fixture/factory used in this repo

---

## add-domain

**When it fires**: agent is adding a new entity, bounded context, or major
domain concept — not just a new endpoint on an existing resource.

**Always-in-context goal**: agent has the complete creation checklist and
knows the correct order without having to reverse-engineer from existing code.

### Content sources

| Section | Source artifact | Source section |
|---|---|---|
| Domain creation checklist | `domain.md` | Core Entities + Core Operations |
| Schema/migration workflow | `ops.md` | Database section |
| Entity structure | `technical.md` | Patterns in Use (Repository pattern) |
| Dependency wiring | `architecture.md` | Component Map + Layers |
| Existing domain as reference | `domain.md` | Core Entities (pick one to use as example) |

### Generated SKILL.md structure

```markdown
---
name: {repo-slug}-add-domain
description: >
  Domain-addition workflow for {repo-name}. Use when creating a new
  entity, aggregate, or bounded context. Covers: the domain-creation checklist
  for the {arch-style} architecture (schema → entity → repository → service →
  route), migration commands, and how to use {ExistingEntity} as a reference.
  Load when you know WHAT domain concept to add and need the creation sequence.
---

# {repo-name} — Add a Domain

## Creation checklist

Run in order. Don't skip — each step sets up the next. **This checklist is a
has-DB, layered example. First confirm from the survey that this repo actually
persists to a database and registers components in a DI container. If it does
NOT, strip the migration / repository / DI steps entirely and replace them with
the repo's real persistence and wiring (e.g. a struct + in-memory store + `mod`
declaration for a no-DB Rust/Go service). Citing a migration command in a repo
with no database is a bug.**

- [ ] **Define the entity/type** in `{domain-path}/` — attributes, lifecycle states, relationships
- [ ] **Persistence (only if the repo has a database)** — `{migration-create-command}`
      → edit the generated file → `{migration-run-command}`. **No DB → skip; use
      the repo's real store (in-memory map, file, external API client).**
- [ ] **Create the repository/store (if the repo uses one)** in `{repos-path}/` —
      the real access methods (e.g. findById, save, findAll, delete)
- [ ] **Create the service/use-case** in `{services-path}/` — core operations
- [ ] **Create the entry points** in `{handlers-path}/` (routes, CLI subcommand, or exported fns)
- [ ] **Wire it in (only if this repo has explicit wiring)** — DI at `{di-path}`,
      a registration slice, or a `mod`/import at the composition root
- [ ] **Export types** at `{types-barrel-path}` (if the repo uses a barrel/re-export)
- [ ] **Write tests** — see `../write-tests/SKILL.md`

## Reference entity: {ExistingEntity}

Use this as your template. It's the clearest existing example.

| What | Where |
|---|---|
| Entity definition | `{entity-file}` |
| Repository | `{repo-file}` |
| Service | `{service-file}` |
| Entry points | `{handlers-file}` |
| Persistence/migration (if any) | `{migration-file}` |
| Tests | `{test-file}` |

## Persistence

**Include this section ONLY if the repo has a database.** Use the repo's real
migration tooling and commands from ops.md — do not template Prisma/TypeORM/
Alembic/`migrate` syntax unless that tool is actually present.

```bash
# Create a new migration
{migration-create-command} --name {entity-name}

# Apply migrations
{migration-run-command}

# Roll back last migration
{migration-rollback-command}
```

Schema conventions: {what columns are standard, e.g. `id UUID PRIMARY KEY`, `created_at`, `updated_at`}

**No database?** Replace this whole section with how the repo actually stores
state (in-memory collection, flat file, embedded KV, external service) and cite
the real store from the analysis.

## Entity structure

```{lang}
// Template — adapt to your domain concept and language
{entity-template-code-block}
// (From {example-entity-file})
```

## Load for deeper context

(see Shared section below)
```

### Quality checks

- [ ] Checklist steps are in the correct order for this specific repo
- [ ] Migration / repository / DI steps are present ONLY if the repo actually has
      a database / repositories / a DI container — otherwise replaced with the real ones
- [ ] Reference entity is the simplest / clearest existing one (not the most complex)
- [ ] All commands are real (from ops.md — no `<placeholder>` tokens)
- [ ] Entity template code comes from actual code in the repo, in the repo's language
- [ ] The wiring step names the repo's real wiring mechanism (or is omitted if none)

---

## write-tests

**When it fires**: agent is writing new tests, fixing failing tests, or
adding test coverage.

**Always-in-context goal**: agent knows the test structure, how to set up
fixtures, and what the right level of testing is for each type of code.

### Content sources

| Section | Source artifact | Source section |
|---|---|---|
| Test framework setup | `technical.md` | Testing Patterns |
| Test commands | `ops.md` | Daily Development Commands |
| Fixture/factory patterns | `technical.md` | Testing Patterns → test data |
| What to test | `domain.md` | Domain Rules & Invariants |
| Test file conventions | `technical.md` | Conventions |

### Generated SKILL.md structure

```markdown
---
name: {repo-slug}-write-tests
description: >
  Testing workflow for {repo-name} — a {type} using {test-framework}.
  Use when writing new tests, fixing failing tests, or adding coverage.
  Covers: test structure ({co-located/separate}), fixture setup with {FactoryName},
  what to test at each layer, and all test commands.
  Load this before writing any test in {repo-name}.
---

# {repo-name} — Write Tests

## Test commands

```bash
{test-all}                          # run everything
{test-unit}                         # unit tests only
{test-integration}                  # integration tests only
{test-watch}                        # watch mode
{test-specific} {path-or-pattern}   # one file or pattern
{test-coverage}                     # with coverage report
```

## Test structure

- **Location**: {co-located with source / in `{test-dir}/` / both}
- **Naming**: `{file}.{test|spec}.{ext}` (e.g. `user.service.test.ts`)
- **Framework**: {test-framework} + {assertion-library}

## Setting up test data

```{lang}
// Use {FactoryName} for domain objects:
{factory-example}

// Use {MockName} for external dependencies:
{mock-example}
```

Factory location: `{factory-path}`

## What to test at each layer

Rows below are a layered-web example. **Use the repo's real units from
architecture.md — for a CLI use `command`/`package`; for a library use the
public modules; drop the Repository row if there is no data layer.**

| Layer / unit | Test type | What to cover | Example file |
|---|---|---|---|
| Domain / core | Unit | Business rules, invariants, state transitions | `{domain-test-example}` |
| Service / use-case | Unit (+ test doubles) | Orchestration, auth checks, error paths | `{service-test-example}` |
| Handler / command | Integration | Input validation, output shape, error mapping | `{handler-test-example}` |
| Repository / store | Integration | Query/store correctness, not-found handling | `{repo-test-example}` |

## Domain rules that MUST have test coverage

From domain analysis — these are the invariants:
{list of top 5-7 domain rules from domain.md, each one is a test scenario}

- `{rule}` → test: `{test-scenario-description}`
- ...

## Load for deeper context

(see Shared section below)
```

### Quality checks

- [ ] All commands are real and copy-pasteable
- [ ] Factory/mock examples come from real code in the repo
- [ ] Domain rules list comes from domain.md verbatim (not invented)
- [ ] Layer test table references real existing test files

---

## scaffold

**When it fires**: agent is creating a new component, module, or service
from scratch and needs the file structure to bootstrap.

**Always-in-context goal**: agent has the right file/folder structure and
can create every file in the right place with the right boilerplate.

### Content sources

| Section | Source artifact | Source section |
|---|---|---|
| File structure | `architecture.md` | Layers + Component Map |
| Naming conventions | `technical.md` | Conventions |
| Boilerplate patterns | `technical.md` | Key Patterns in Use |
| DI registration | `architecture.md` | Cross-Cutting Concerns |
| Reference component | survey.md + technical.md | Key Files |

### Generated SKILL.md structure

```markdown
---
name: {repo-slug}-scaffold
description: >
  Scaffolding guide for {repo-name}. Use when creating a new
  component, module, or service from scratch. Provides the correct file
  structure, naming conventions, and boilerplate patterns for this {arch-style}
  codebase. Load before creating any new files in {repo-name}.
---

# {repo-name} — Scaffold a Component

## File structure for a new {module/service/domain-area}

**Mirror the layout of an existing component in THIS repo — copy the real file
set, extensions, and naming, not the example below.** The shape varies sharply
by stack:

```
# TS feature module (the example):
{module-name}/
├── {module-name}.handler.ts      ← route handler
├── {module-name}.service.ts      ← business orchestration
├── {module-name}.schema.ts       ← request/response types
├── {module-name}.test.ts         ← tests (if co-located)
└── index.ts                      ← barrel export

# Go tool/package: often one file + its test, no folder:
{name}_tool.go
{name}_tool_test.go

# Rust module:
{module_name}/
├── mod.rs                        ← module root + re-exports
└── {module_name}.rs              ← impl (+ #[cfg(test)] mod tests)
```

(Adapt names to the repo's real convention — see Naming conventions below.)

## Naming conventions

| Thing | Convention | Example |
|---|---|---|
{conventions-table-rows}

## Minimal boilerplate — {MainClass/MainFn}

```{lang}
{minimal-boilerplate}
// (Pattern from: {example-file})
```

## Wire it in

**Only if this repo has an explicit registration step.** Show the repo's real
mechanism — a DI container, a registration slice in `server.go`, or a `mod` /
import at the composition root. **If components are auto-discovered or there is
no wiring, delete this section.**

```{lang}
// In {wiring-path}:
{wiring-example}
```

## Checklist

- [ ] Created files in `{correct-path}/` matching an existing component's layout
- [ ] Names follow `{naming-convention}` (the repo's real convention)
- [ ] Wired in at `{wiring-path}` (only if the repo has a wiring step)
- [ ] Exported from barrel/`mod` at `{barrel-path}` (if applicable)
- [ ] Test file created

## Load for deeper context

(see Shared section below)
```

---

## Shared: "Load for deeper context" table

Every task skill ends with this table. Rows vary by skill — include only
the rows that are relevant for the typical workflow of that skill.

```markdown
## Load for deeper context

When this step needs it, load the relevant wiki ref:

| I need to understand... | Load |
|---|---|
| Architecture: layers, components, data flow | `../{repo-slug}-wiki/refs/arch.md` |
| Domain model: entities, rules, operations | `../{repo-slug}-wiki/refs/domain.md` |
| API surface: endpoints, auth, error format | `../{repo-slug}-wiki/refs/api.md` |
| Recurring design patterns and how to add instances | `../{repo-slug}-wiki/refs/patterns.md` |
| Setup, migration, environment config | `../{repo-slug}-wiki/refs/onboard.md` |
| Operational runbooks: build, run, recover | `../{repo-slug}-wiki/refs/ops.md` |
| Product context: what the system does | `../{repo-slug}-wiki/refs/overview.md` |
```

**Canonical ref names** — link ONLY these (each has a producing wiki article
type): `overview.md`, `arch.md`, `api.md`, `onboard.md`, `domain.md`,
`patterns.md`, `ops.md`. Never invent a ref filename or hardcode one the wiki
doesn't produce — that ships a dead link.

**Guidance**: include only the rows the agent would realistically need for
this task — not all of them on every skill. A bug-fix skill needs arch +
domain + api (+ ops for diagnostics). A write-tests skill needs domain (for
rules) + onboard (for commands). An api.md row is dead weight on a repo with
no API; a patterns.md row is dead weight if the wiki found no recurring
pattern — drop rows whose ref the wiki didn't generate for this repo.

---

## Shared: description formula

Every generated skill description must follow this formula, emitted as a YAML
**folded block scalar** (`description: >`):

```
description: >
  {Task} workflow for {repo-name} — a {type} built with {stack}.
  Use when {specific trigger condition}.
  Covers: {3 specific things this skill knows about this repo}.
  Load {before/when} {specific agent action} in {repo-name}.
```

The generated `description:` MUST be a folded block scalar (`>`), because the
formula contains `: ` (`Covers:`, `Use when …:`) which breaks a plain YAML
scalar — `npx skills` throws "Nested mappings are not allowed in compact
mappings" and SILENTLY DROPS the skill (exit 0, skill never installs). The `>`
on its own line, with the text indented beneath it, makes every colon in the
body literal and safe.

The third sentence MUST name specific, repo-specific facts — not generic
descriptions. Pull the facts from the repo's actual stack/shape, e.g.:
- TS/REST: "the Zod-validated request pipeline, hexagonal layer triage, and pnpm test commands"
- Go CLI/server: "the tool-registration step in `server.go`, `error`-return propagation, and `go test ./...`"
- Rust library: "the trait-impl wiring in `lib.rs`, `Result`/`?` error flow, and `cargo test`"

Bad (name-swappable, fits any repo): "how to fix bugs and write tests".
